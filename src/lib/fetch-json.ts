/**
 * Re-export from fetch-helpers.ts for backward compatibility.
 * New code should import from fetch-helpers.ts directly.
 */
export { fetchJson, fetchJsonOrNull, safeParseJson, FetchError } from './fetch-helpers';
