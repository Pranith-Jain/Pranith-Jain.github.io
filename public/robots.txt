# robots.txt for Pranith Jain Portfolio
# https://pranithjain.qzz.io

User-agent: *
Allow: /

# Sitemap location
Sitemap: https://pranithjain.qzz.io/sitemap.xml

# Disallow any potential admin or private areas (not applicable for this static site)
Disallow: /admin
Disallow: /private/
Disallow: /api/

# Binary asset directories — large WASM, gzip, and document files that
# Google Safe Browsing flags as "harmful downloads" or "uncommon downloads".
# These are runtime dependencies for client-side features (OCR, resume),
# not downloadable content, but Google's heuristic treats them as such.
Disallow: /tesseract/
Disallow: /resume.docx
Disallow: /*.docx
Disallow: /*.gz
Disallow: /*.wasm
Disallow: /*.ps1
Disallow: /data/si/scripts/

# Malware/sample-related pages — Google Safe Browsing flags these as
# "uncommon download" or "malware" warnings. These are analysis tools,
# not download sites, but Google's heuristic treats them the same.
Disallow: /threatintel/malware-vault
Disallow: /threatintel/malware-sandbox
Disallow: /threatintel/malware/vault
Disallow: /threatintel/malware/sandbox
Disallow: /dfir/malware-scan
Disallow: /dfir/sample-scan
Disallow: /dfir/sandbox

# AI answer-engine crawlers — explicitly allowed so blog/threat-intel content
# is eligible to be cited in AI Overviews / ChatGPT / Perplexity / Claude /
# Gemini / Copilot / Perplexity answers. Blocking any of these would make the
# portfolio invisible to that engine. A named group does NOT inherit the `*`
# rules, so the private-area disallows are repeated for each block below.
# Three categories of AI crawlers:
#   1. Training crawlers (GPTBot, ClaudeBot, anthropic-ai, Google-Extended,
#      CCBot, Bytespider, Amazonbot, FacebookBot) — block = future model
#      versions won't know about the site.
#   2. Retrieval crawlers (OAI-SearchBot, PerplexityBot, Applebot-Extended,
#      GoogleOther) — block = immediate loss of citations from that engine.
#   3. Live browsing agents (ChatGPT-User) — block = one-off user fetches fail.

# --- OpenAI: training + retrieval + live browsing ---
User-agent: GPTBot
User-agent: OAI-SearchBot
User-agent: ChatGPT-User
Allow: /
Disallow: /admin
Disallow: /private/
Disallow: /api/

# --- Anthropic: training + retrieval ---
User-agent: ClaudeBot
User-agent: Claude-Web
User-agent: anthropic-ai
Allow: /
Disallow: /admin
Disallow: /private/
Disallow: /api/

# --- Google: AI products (separate from Googlebot search) ---
User-agent: Google-Extended
User-agent: GoogleOther
Allow: /
Disallow: /admin
Disallow: /private/
Disallow: /api/

# --- Perplexity: real-time retrieval ---
User-agent: PerplexityBot
Allow: /
Disallow: /admin
Disallow: /private/
Disallow: /api/

# --- Apple Intelligence ---
User-agent: Applebot-Extended
Allow: /
Disallow: /admin
Disallow: /private/
Disallow: /api/

# --- ByteDance / TikTok: training ---
User-agent: Bytespider
Allow: /
Disallow: /admin
Disallow: /private/
Disallow: /api/

# --- Common Crawl: open training dataset ---
User-agent: CCBot
Allow: /
Disallow: /admin
Disallow: /private/
Disallow: /api/

# --- Amazon Alexa AI ---
User-agent: Amazonbot
Allow: /
Disallow: /admin
Disallow: /private/
Disallow: /api/

# --- Meta AI ---
User-agent: FacebookBot
Allow: /
Disallow: /admin
Disallow: /private/
Disallow: /api/

# --- llms.txt for AI model context ---
# Served at https://pranithjain.qzz.io/llms.txt — a curated Markdown map
# of the portfolio's most important pages for AI assistants and answer engines.
# Complementary file to robots.txt above; not a crawler directive itself.
